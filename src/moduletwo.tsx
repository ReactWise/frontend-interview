export default function ModuleTwo() {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <div className="bg-white p-8 rounded-lg shadow-md">
        <h1 className="text-2xl font-bold text-gray-800 mb-4">
        Module Two: Change inner circle red on hover inner/outer circle.
        Then, show unique information in box when hover. (any, so long as it correlates with highlighted circle)
        </h1>
      <img src="/src/assets/moduletwo.png" alt="Module Two" />
      </div>
    </div>
  );
}
